[project]
name = "proj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "neo4j>=5.0.0",
    "pandas>=2.0.0",
    "pre-commit>=4.5.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]
