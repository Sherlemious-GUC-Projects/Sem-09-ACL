[project]
name = "proj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nltk>=3.9.2",
    "pre-commit>=4.5.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]

[tool.mypy]
ignore_missing_imports = true
